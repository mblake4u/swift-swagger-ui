# #This version of Swagger UI docker-compose.yml to update index.html and the Swift API YAML, but because App Runner ignores docker-compose.yml it shows the Pet Store when running there.
# Status: This works locally (docker build), but App Runner shows the Pet Store
# Issues: 
# To Do: I'm not going to do anything with this version